---
id: parseFrontmatter
title: parseFrontmatter
---

# Function: parseFrontmatter()

```ts
function parseFrontmatter(filePath): Record<string, unknown>;
```

Defined in: [utils.ts:25](https://github.com/TanStack/intent/blob/main/packages/intent/src/utils.ts#L25)

Parse YAML frontmatter from a file. Returns null if no frontmatter or on error.

## Parameters

### filePath

`string`

## Returns

`Record`\<`string`, `unknown`\>
