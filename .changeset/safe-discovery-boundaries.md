---
'@tanstack/intent': patch
---

Reject unsafe dynamic identifiers in runnable list commands, install mappings, and runtime lookup hints. Skip skill metadata that resolves outside its package root while preserving valid package-manager symlinks and direct-load containment errors.
